import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
    container: {
        paddingTop: 50,
        flex: 1,
        alignItems: 'center',
    },
    loginInput: {
        marginBottom: 30,
    },
    passwordInput: {
        marginBottom: 60,
    },
    registerBtn: {
        marginTop: 40,
    },
});
