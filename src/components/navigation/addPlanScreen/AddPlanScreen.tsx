import Layout from '../../common/layout/Layout';
import {View} from 'react-native';

const AddPlanScreen = () => {
    return (
        <Layout hasBackArrow bgImageType={'right-center'} customStyle={{paddingHorizontal: 20}}>
            <View style={{height: 300}}></View>
        </Layout>
    );
};
export default AddPlanScreen;
