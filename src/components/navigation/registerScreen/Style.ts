import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    header: {
        paddingTop: 50,
        flex: 1,
        alignItems: 'center',
    },
    emailInput: {
        marginBottom: 20,
    },
    passwordInput: {
        marginBottom: 20,
    },
    repeatPasswordInput: {
        marginBottom: 60,
    },
});
