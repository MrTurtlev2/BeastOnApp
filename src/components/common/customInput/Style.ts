import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    wrapper: {},
    input: {
        backgroundColor: 'red',
        minWidth: 250,
        borderRadius: 20,
        textAlign: 'center',
        paddingHorizontal: 15,
    },
});
